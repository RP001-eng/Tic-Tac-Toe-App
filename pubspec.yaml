name: tictacnew
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0' # Updated minimum SDK version

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6 # Corrected and updated version
  firebase_core: ^2.24.2 # Updated version
  firebase_auth: ^4.16.0 # Updated version
  cloud_firestore: ^4.14.0 # Updated version
  confetti: ^0.7.0



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Updated version

flutter:
  uses-material-design: true
  assets:
    - assets/google_icon.png
    - assets/pic.jpg
    - assets/tap.mp3
    - assets/win.mp3
    - assets/draw.mp3